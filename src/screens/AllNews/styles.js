import {StyleSheet} from 'react-native';
import {hp, wp} from '../../common/utils';

export const AllNewsaStyles = StyleSheet.create({
  box: {
    marginTop: hp(20),
    paddingHorizontal: wp(10),
  },
  indicator: {
    marginTop: hp(20),
  },
});
